########Name:Dev Stack Builder Website

Description : A simple project where has a navbar, a banner section , a functional card section where has a buttton add and remove system and a footer section

used technology: React,tsx,react-toast,daisyUi,tailwind css

featchers:_/Brows different technology with their name , catagory,descriptuion ,rating and icons.
_/add favorite technology to a stack list
\*/Remove technology individually and remuve all as own wish---------------------------------------------------

###########

1. Jsx is external java script where we use html element easily......It makes React components easier to read and helps us describe what the UI should look like...

2. Props are data passed from a parent component to a child component.

---

State is data managed inside a component that can change over time.

3. we use useState for store and update data ----------------- i use this in my AllCard section and when a card add or remove than it is use

4. useEffect is used to perform side effects in a React component, such as fetching data from an API.---------------

If we load JSON data using useEffect, we can fetch the data when the component loads and then store it in state.

5. key is important for indentify element individually-----

6. Conditional rendering means showing something in the UI only when a certain condition is true.--------- we can see this in card.tsx-------

7. A parent sends data to a child using props.------The child can send something back to the parent by calling a function passed through props.
